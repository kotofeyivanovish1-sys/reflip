[build]
builder = "nixpacks"
buildCommand = "NODE_OPTIONS=--max-old-space-size=1536 npm install && NODE_OPTIONS=--max-old-space-size=1536 npm run build"

[deploy]
startCommand = "node dist/index.cjs"
healthcheckPath = "/api/health"
restartPolicyType = "ON_FAILURE"

[service]
internalPort = 5000
